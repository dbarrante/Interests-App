There's no `pnpm` setup in this project — it's a zero-tooling vanilla web app (no package.json, no dependencies). The tests that exist are plain Node scripts.

Let me run them directly:

<bash>
node tests/syntax-check.js
</bash>

<bash>
node tests/durability.test.js
</bash>