# v1.9.0 Tightening Implementation Plan (Phase 3)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement Phase 3 of `docs/full-review-2026-07-02.md` — eliminate the big duplication clusters (sections E, F), begin the index.html decomposition into tested pure modules, and clear the deferred-minor backlog from v1.7.0/v1.8.0, shipping as app v1.9.0 / extension v4.48. Behavior-preserving refactors: the app should work IDENTICALLY after every task.

**Architecture:** Core duplications collapse into small `core/` modules (the pattern the codebase already uses). Web duplications collapse into `web/lib/*.js` pure dual browser/Node modules (the established `route-capture.js` pattern: plain `<script src>` tags, CommonJS export for tests, no bundler). No schema changes; no endpoint changes; `.img`/`.image` unification is explicitly DEFERRED to Phase 4 (iPhone schema work).

**Tech Stack:** unchanged (Node core, vanilla JS renderer, MV3 extension, plain-Node tests).

**Plan-style note (pre-approved deviation, validated across Phases 1-2):** refactor tasks specify exact extraction targets, invariants, and acceptance criteria rather than verbatim code — implementers must read the current state first. TDD everywhere a harness exists; every extraction lands WITH tests for the new module; behavior-equivalence is the reviewers' core check.

## Global Constraints

- Base: master at `664009f` (v1.8.0). Branch: `v1.9.0-tightening`.
- BEHAVIOR-PRESERVATION RULE: these are refactors. Any observable behavior change (request shapes, error responses, filter results, capture outcomes, UI text) must be zero unless a task explicitly sanctions it — reviewers reject drift.
- Every extraction: grep-verified zero copies left behind; the new module gets its own plain-Node test file; the old call sites are updated in the same commit.
- Data-safety invariants (standing): `persistCards()` / `{confirm:true}` semantics untouched; `_booted` gating untouched; asOf watermark untouched.
- Tests: plain Node; network-free (loopback ok); no `process.exit` in new tests; run touched files while iterating, full `npm test` once per task before commit; the controller independently re-runs the suite at release.
- Commits end with: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`
- Version bumps (app 1.9.0, ext 4.48) ONLY in Task 8.
- Reference docs: `docs/full-review-2026-07-02.md` sections E (tightening) and F (split), `docs/BACKLOG.md` v1.8.0 deferred list.

---

### Task 1: `core/guardedfetch.js` — one SSRF-guarded fetch (E, biggest core win)

**Files:** Create `core/guardedfetch.js`, `tests/guardedfetch.test.js`. Modify `core/linkcheck.js`, `core/contentcheck.js`, `core/capturemeta.js` (and their tests only if assertions reference moved internals).

**Spec:**
- [ ] Extract ONE implementation of the triplicated machinery: SSRF-guarded fetch with AbortController+timeout, manual-redirect loop calling `safeToFetch` per hop, `Connection: close`, and the drain-don't-cancel capped body reader (the undici-crash workaround — currently implemented in `contentcheck.js` and `capturemeta.js`; pick the most defensive variant and document why). Also: ONE shared worker-pool helper (three hand-rolled copies) and ONE UA constant.
- [ ] API design is the implementer's (read all three call sites first); suggested shape: `guardedFetch(url, {timeoutMs, maxBytes, maxRedirects, headers})` → `{status, headers, body|buffer, finalUrl}` + `runPool(items, concurrency, workerFn)`. The three modules keep their public exports and per-module semantics (probe vs content vs meta differ deliberately — only the transport unifies).
- [ ] INVARIANTS (reviewer checks): every redirect hop still passes `safeToFetch` (SSRF); the capped reader still drains-not-cancels (undici crash); per-module timeouts/caps keep their current values (grep them out and preserve exactly); linkcheck's conservative status semantics unchanged.
- [ ] Tests: new module tested directly against a local `http.createServer` on 127.0.0.1 (redirect chains incl. a hop to a blocked address → rejected; body cap; timeout) — follow how tests/linkcheck*.test.js/contentcheck tests already fake servers. Existing linkcheck/contentcheck/capturemeta tests must pass UNCHANGED (they are the behavior-equivalence net).
- [ ] Commit: `refactor(core): one guardedfetch module replaces three SSRF-fetch copies (review E)`

### Task 2: Core small tightenings (E)

**Files:** Modify `core/db.js`, `core/merge.js`, `core/server.js`, `core/config.js`, `core/sync.js`, `preload.js`, `web/index.html` (window.ia only), tests as needed.

**Spec (each grep-verified):**
- [ ] `_stable` stringify exists in `db.js` and `merge.js` and MUST stay in lockstep (content signatures) — single source: export from ONE place both can reach; merge.js is dual browser/Node and must stay dependency-free in the browser... so: keep the function in merge.js (pure, no deps), have db.js require it from `./merge.js`. Add a test asserting `db`'s sig path and `merge`'s sig path produce identical output for a sample card.
- [ ] `core/server.js`: one `jsonKvEndpoints(app, route, kvKey)` helper replacing the three copy-pasted GET/POST kv route pairs (capture-request / batch-state / batch-progress) — byte-identical request/response shapes; dedupe the doubled imports at the top (dbm+counts, images+imageCount, extensionful `"./backup.js"`); add a JSON error middleware LAST in the chain that returns `{ok:false, error:"internal"}` (500) WITHOUT the stack trace, logging the real error server-side — sanctioned behavior change: error bodies stop leaking stacks; success shapes unchanged.
- [ ] `core/config.js`: rename side-effecting getter `getSyncConfig` → keep `getSyncConfig` as the name BUT split: `ensureSyncConfig()` (writes defaults) called at startup in main.js; `getSyncConfig()` becomes read-only. All callers audited (main.js, synctimers deps, server routes).
- [ ] `core/sync.js`: fix `copyByeId` typo → `copyById`; delete the stray `void out;`.
- [ ] `preload.js` + `web/index.html`: consolidate the dual `window.ia`/`window.app` bridge to `window.ia` only — grep index.html for every `window.app`/`app.` bridge usage and repoint; keep a one-line `window.app = window.ia` alias ONLY if any dynamic access pattern makes static repointing unsafe (justify in report).
- [ ] Tests: extend existing config/server tests for the split getter and error middleware (a route that throws → 500 `{ok:false,error:"internal"}`, no stack); kv endpoint tests must pass unchanged.
- [ ] Commit: `refactor(core): shared stable-stringify, kv-endpoint helper, error middleware, read-only getSyncConfig, bridge name unification (review E)`

### Task 3: `web/ai.js` — one AI dispatch (E, biggest web win)

**Files:** Create `web/ai.js`, `tests/ai-module.test.js`. Modify `web/index.html` (+`<script src>`), possibly `web/deadcheck-ai.js`/`web/profile-analyze.js` ONLY if they duplicate the parse helper (check; prefer untouched).

**Spec:**
- [ ] Move the six provider callers (`callAnthropic`, `callOpenAI`, `callGemini`, `callGroq`, `callOpenRouter`, `callLocal`) out of index.html into `web/ai.js`, plus: `callAI(prompt, opts?)` — the single dispatcher replacing the `{anthropic:callAnthropic,...}[S.provider]` object copy-pasted ~10×; `hasAIKey()` replacing the ~9 no-key guard variants (pick ONE user-facing message; sanctioned tiny copy change); `parseJsonArray(text)` replacing the 6 copies of strip-fences → bracket-slice → JSON.parse.
- [ ] Module reads settings via an injected accessor (e.g. `IA_AI.configure(() => S)` called once at boot) — NOT a global S reference — so the module stays testable in Node with a fake settings object. Browser-side: attach as `root.IA_AI`; CommonJS export for tests (route-capture.js pattern). fetch is used inside — gate the provider callers on `typeof fetch` like storage.js does; `parseJsonArray` stays pure.
- [ ] Replace ALL index.html call sites (grep `callAnthropic|callOpenAI|callGemini|callGroq|callOpenRouter|callLocal|\[S\.provider\]` → zero outside ai.js; grep the fence-strip pattern → zero).
- [ ] Tests: `parseJsonArray` cases (fenced, unfenced, junk-wrapped, malformed → null/throw per current behavior — READ one current copy and preserve its contract exactly); `callAI` dispatch + no-key path with a fake settings accessor and a stubbed fetch.
- [ ] Commit: `refactor(web): web/ai.js — one AI dispatcher + parseJsonArray replace ~10 copies (review E)`

### Task 4: `web/lib/urlkey.js` — one URL-canonicalization module (E)

**Files:** Create `web/lib/urlkey.js`, `tests/urlkey.test.js`. Modify `web/index.html` (+script tag).

**Spec:**
- [ ] Consolidate the four canonicalizers — `urlKey` (feed dedupe), `normalizeUrl` (FB redirect unwrap etc.), `dupeUrlKey` (duplicate scan), `clipKey` (clip dedupe incl. YouTube-shorts handling) — into one module. The four have DELIBERATE semantic differences: preserve each as a named export (`feedKey`, `normUrl`, `dupeKey`, `clipKey`) built on shared internals; FIX the one known inconsistency (review 2.5): duplicate-scan and clip-dedupe disagree about YouTube shorts — align `dupeKey` to `clipKey`'s shorts handling (sanctioned behavior change: duplicate scan groups shorts correctly; document it).
- [ ] Table in the report: each key fn → its exact transform list (host lowering, www, hash, query, trailing slash, FB unwrap, shorts) — derived from the current code, this becomes the test matrix.
- [ ] Replace all index.html call sites; grep the old names to zero (keep thin aliases in index.html ONLY if inline-onclick strings reference them — prefer zero).
- [ ] Tests: the transform matrix as cases, incl. the FB-unwrap and shorts cases and cross-fn disagreement cases pinning the intended differences.
- [ ] Commit: `refactor(web): web/lib/urlkey.js — four URL canonicalizers, one tested module; dupe-scan agrees with clip-dedupe on shorts (review E)`

### Task 5: `web/lib/` — import parsers + capture-state predicates (F, begin the split)

**Files:** Create `web/lib/import-parsers.js`, `web/lib/capture-state.js`, tests for both. Modify `web/index.html` (+script tags).

**Spec:**
- [ ] `import-parsers.js`: move the pure parsing logic out of index.html — `parseFacebookJSON`, the CSV parsing (REPLACE index.html's `splitCsv` — which mishandles doubled quotes — with the correct `splitCsvLine` logic that already exists in `web/import-google-saved.js`; sanctioned fix: YouTube CSV titles with quotes stop being mangled; do NOT modify import-google-saved.js itself), `parsePinterestSAR`, the `harvest`/`clean`/`normTs` helpers, and `ingestImported`'s pure dedupe core (the id/url-key dedupe decision, NOT the array mutation/persist part — that stays in index.html and calls the pure core). DOM-free: anything touching document/Store stays behind.
- [ ] `capture-state.js`: move the card-state predicates — `needsCapture`, `needsRetry`, `fbMiss`, `captureable`, `isBadImg`, `needsFbCapture` (grep for the actual current set; Task 6 of Phase 2 used them for the panel buckets) — as pure functions of a card object. index.html callers (renderImported filter, panel buckets, dispatchers) repoint. These predicates gate captures and filters: byte-equivalent logic, verified by the reviewer term-by-term (the Phase-1 B8 lesson).
- [ ] Both modules: dual browser/Node (route-capture.js pattern).
- [ ] Tests: parser fixtures (a small FB export snippet, CSV with doubled quotes, Pinterest SAR snippet — synthetic, no real personal data) + predicate truth tables (cards in each state).
- [ ] Commit: `refactor(web): extract tested import-parsers + capture-state modules from index.html (review F); CSV doubled-quote fix`

### Task 6: One parameterized batch dispatcher (E)

**Files:** Modify `web/index.html`. Depends on Tasks 4-5 (uses clipKey + predicates from the modules).

**Spec:**
- [ ] Consolidate the four near-identical batch dispatchers — `recaptureViaWorker`, `startFbCapture`, `startFbMissRenderCapture`, and the retry-stamping path inside `startBatchCapture` — into one `dispatchCaptureBatch(cards, opts)` where opts covers: candidate filter, cap, `fb`/`render`/`force` flags, delay/pacing. Each old name becomes a thin wrapper (the Get-pictures panel and Select→Recapture call sites stay unchanged) OR call sites repoint directly — implementer's choice, zero-copies-left either way.
- [ ] INVARIANTS: dedupe-by-clipKey, ensure-ids, `lastUpdate`/`lastResult="pending"` stamping, `Store.setBatchState` shape, `batchUI` fields, toasts — all byte-equivalent per path (the four current bodies differ subtly; the report documents each difference and where it went). The panel's drain loop and Stop semantics (P2 T6) must be re-traced after the change.
- [ ] Tests: `tests/capture-wiring.test.js` source assertions updated meaningfully (helper contains the shared logic; wrappers delegate) — the Phase-1 Task 8 pattern.
- [ ] Commit: `refactor(web): one parameterized dispatchCaptureBatch replaces four dispatcher copies (review E)`

### Task 7: Deferred-minors sweep (v1.7.0/v1.8.0 backlogs + review L-items)

**Files:** Modify `extension/background.js`, `web/index.html`, `core/db.js`, `core/config.js` (comment only), `web/restore-legacy.js` if needed.

**Spec (each independently small; grep first, skip-with-note if stale):**
- [ ] Extension: add a re-entrancy guard to `pollCaptureRequest` (concurrent claims clobbering B12 persistence — mirror `pendingCaptureBusy` semantics); add `flushQueue()` to `iaPollAll` (faster offline-queue delivery); fix the stale "driven by the page-side bridge" comment (~759); add the double-delivery-window comment at the poller's `finally`.
- [ ] Web: unify the three Escape listeners into one keydown handler with a top-modal priority order (health/getpic/guide) — one Esc closes ONE topmost surface; delete the web-side `backupCountsMatch` orphan; remove `enrichImported`'s guarded dead `#enrichBtn` reference; `stumbleSave` gets the same 60-cap slice as `stumbleVote`; `parseItems` ids get a collision-proof suffix (counter/random — match `newId()`'s existing approach); `openLink` skips non-http(s) URLs like `openUrlsInTabs` already does.
- [ ] Core: comment the `asOf=0` sentinel in `core/db.js` (fresh-Store keeps everything — intended); wrap `saveConfig`'s rename in try/catch that unlinks the tmp sidecar on failure and rethrows; one-line comment on the corrupt-row sig-bump repair semantics.
- [ ] Tests: extend existing files where the change is testable (config tmp-cleanup failure path; stumble cap if a harness exists — likely not, note it); full suite green.
- [ ] Commit: `chore: deferred-minors sweep from v1.7.0/v1.8.0 reviews (ext poller guard+flush, Esc unification, small web/core fixes)`

### Task 8: Release v1.9.0 / ext 4.48

**Files:** `package.json` (1.9.0), `extension/manifest.json` (4.48), `extension/background.js` `FB_CAP_VERSION` ("4.48"), `docs/BACKLOG.md`.

**Spec:**
- [ ] Bumps; BACKLOG v1.9.0 entry (one line per task; note the three sanctioned behavior changes: error bodies stop leaking stacks, shorts dedupe alignment, CSV doubled-quote fix; note `.img`/`.image` unification deferred to Phase 4); full `npm test` with the verbatim last line in the report (BLOCKED on any failure — no fixing here).
- [ ] Commit: `chore(release): v1.9.0 / ext 4.48 — duplication collapse, module extraction, deferred minors (review Phase 3)`

---

## Self-review notes

- Coverage vs review Phase 3 list: guardedfetch→T1; _stable/jsonKv/error-middleware/imports/getter/typo/bridge-name→T2; callAI+parseJsonArray→T3; canonicalUrl→T4; index.html split (lib modules)→T5 (views/state split intentionally NOT this release — too big; next increment); dispatchCaptureBatch→T6; removeCards→already done (P2 T5); `.img`/`.image`→deferred to Phase 4 (stated); deferred minors→T7.
- Order: T1, T2 independent (both core — sequential anyway). T3 before T6 (dispatchers touch AI? no — but T4/T5 before T6: dispatcher uses clipKey + predicates). T7 last before release (touches files earlier tasks restructure).
- Model guidance: T1, T6 opus (transport/dispatch semantics); T2, T7 sonnet; T3, T4, T5 opus (large index.html surgery with behavior-equivalence stakes); reviewers mirror implementers; final review most capable.
